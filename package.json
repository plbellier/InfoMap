{
    "name": "infomap-root",
    "version": "1.0.0",
    "description": "Root configuration for InfoMap development",
    "private": true,
    "scripts": {
        "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"./.venv/bin/python backend/main.py\" \"npm run dev --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}